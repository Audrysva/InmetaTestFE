import { ServiceNamesPipe } from './service-names.pipe';

describe('ServiceNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new ServiceNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
